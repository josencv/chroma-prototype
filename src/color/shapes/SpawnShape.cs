namespace ChromaPrototype.Color.Shapes;

/// <summary>
/// Defines the shape type for probe field spawning.
/// </summary>
public enum SpawnShape
{
    /// <summary>
    /// Spawn probes in a circular/disk pattern on the XZ plane.
    /// </summary>
    Disk,

    /// <summary>
    /// Spawn probes in a rectangular grid on the XZ plane.
    /// </summary>
    Rect
}
